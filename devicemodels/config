// channel used to publish updates on vehicle data as received by the /etl/loader
var channel = "responseChannel";
var autoCreate = true;

function getConfig(){
  return {
    "autoCreate" : autoCreate
  }
}