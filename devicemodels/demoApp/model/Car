import("model.Vehicle")
//import("model.Wheel")

var Car = Class(Vehicle, {
  constructor: function(model) {
    Car.$super.call(this, model)
    this.set("cls", "model.Car") 
    this.set("seats", 4)
  }
})
